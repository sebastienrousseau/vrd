{{!--
  SPDX-License-Identifier: Apache-2.0 OR MIT
  Handlebars template consumed by `cargo about generate` to
  produce NOTICE. Run via `make notice`.
--}}
# Third-party notices for vrd

vrd redistributes the following third-party crates. Each carries
its own license; the text reproduced below is the canonical
license at the version pinned by `Cargo.lock`.

Generated by `cargo about generate` from `about.toml` + this
template. Re-run `make notice` whenever the dependency tree
changes.

{{#each overview}}
- **{{name}}** ({{count}}): {{join licenses ", "}}
{{/each}}

---

{{#each licenses}}
## {{name}}

Used by:

{{#each used_by}}
- [`{{crate.name}}` v{{crate.version}}](https://crates.io/crates/{{crate.name}}/{{crate.version}})
{{/each}}

```
{{{text}}}
```

---
{{/each}}
